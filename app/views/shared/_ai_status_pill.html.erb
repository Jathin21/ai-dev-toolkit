<% klass = case pr.ai_status
            when "completed" then "pill-ok"
            when "failed"    then "pill-err"
            else                  "pill-warn"
            end %>
<span class="<%= klass %>">AI: <%= pr.ai_status %></span>
